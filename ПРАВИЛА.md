# ПРАВИЛА РАЗРАБОТКИ — ПРИОРИТЕТ #1

## ⛔ ПРАВИЛО №1 (АБСОЛЮТНЫЙ ПРИОРИТЕТ): НЕЛЬЗЯ УДАЛЯТЬ РАБОТЫ ПОЛЬЗОВАТЕЛЕЙ

**Это правило нарушать НЕЛЬЗЯ ни при каких обстоятельствах.**

### Что запрещено:
- ❌ Перезаписывать `library/index.json` с меньшим количеством записей
- ❌ Заменять `library/index.json` содержимым из скрипта который обработал только часть записей  
- ❌ Запускать batch-скрипты которые могут перезаписать index.json с потерей записей
- ❌ Делать `git push --force` с урезанным index.json
- ❌ Любые операции которые могут привести к потере данных пользователей

### Что нужно делать ПЕРЕД любой операцией с library/index.json:
1. ✅ Проверить количество записей ДО операции: `python3 -c "import json; print(len(json.load(open('library/index.json'))))"`
2. ✅ Сделать резервную копию: `cp library/index.json /tmp/index_backup_$(date +%s).json`
3. ✅ Проверить количество записей ПОСЛЕ операции — должно быть >= исходного
4. ✅ Если количество записей уменьшилось — НЕМЕДЛЕННО восстановить из резервной копии

### Текущее состояние (эталон):
- `library/index.json` должен содержать **160+ записей** (работы всех пользователей)

### Что произошло 2026-07-28 (инцидент):
- Скрипт `batch-export-all.mjs` прочитал index.json, обработал 1 запись и записал обратно index.json только с 1 записью
- Все 159 других работ пользователей исчезли из сайта
- Причина: скрипт делал `fs.writeFileSync(indexPath, ...)` после каждой обработанной записи, перезаписывая оригинальный файл
- Исправление: восстановлен из commit `fce5446ae` (160 записей)

---

**Этот файл должен быть прочитан ПЕРЕД любой работой с library/index.json или пользовательскими данными.**
